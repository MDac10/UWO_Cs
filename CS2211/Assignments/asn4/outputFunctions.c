/*CS2211a 2020*/
/*Assignment 04*/
/*Megan Da Costa*/
/*251009855*/
/*mdacos2*/
/*November 16 2020*/

#include "headers.h"

void strWordOutput(sent *sentence)
{
    /*for(words in large sentence starting from count 0 to sentence.numWords)
        printf("%s\n", *(sentence + count));*/
}

void strLineOutput()
{
    /*for(every sentence in the sentence structure pointer (called pointer) - starting from count 0 to sentence.line)
        printf("%s\n", *(pointer + count));*/

}

void searchWord(char *search) //char *search is a pointer to the original character array
{
    /*for(every sentence in the sentence structure pointer (pointer) - starting from line 0 to sentence.line
        for(words in sentence structure - strating from count 0 to sentence.numWords){
            if(strcmp(search, *(pointer + line).wP) == 0){
                printf("%s found in line %d position %d", search, line, count);
            }
        } 
        if not found
        printf("%s was not found in this file.");
        */
    
}